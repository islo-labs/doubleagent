[project]
name = "slack-contracts"
version = "1.0.0"
description = "Contract tests for Slack fake"
requires-python = ">=3.10"
dependencies = [
    "slack_sdk>=3.27.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
