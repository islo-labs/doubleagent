name: slack
version: "1.0"
description: Slack Web API fake
docs: https://api.slack.com/methods

server:
  command: ["uv", "run", "python", "main.py"]
  port: 8083

contracts:
  command: ["uv", "run", "pytest", "-v", "--tb=short"]

env:
  SLACK_API_URL: "http://localhost:${port}"
  SLACK_BOT_TOKEN: "xoxb-doubleagent-fake-token"

features:
  webhooks: true
