[project]
name = "stripe-contracts"
version = "1.0.0"
description = "Contract tests for Stripe fake"
requires-python = ">=3.10"
dependencies = [
    "stripe>=8.0.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
