[project]
name = "jira-fake"
version = "0.1.0"
description = "Jira API snapshot explorer for DoubleAgent"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "pydantic>=2.0.0",
]
