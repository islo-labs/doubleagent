[project]
name = "posthog-contracts"
version = "1.0.0"
description = "Contract tests for PostHog fake"
requires-python = ">=3.10"
dependencies = [
    "posthog>=3.0.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
