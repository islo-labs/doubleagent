[project]
name = "resend-contracts"
version = "1.0.0"
description = "Contract tests for Resend fake"
requires-python = ">=3.10"
dependencies = [
    "resend>=2.0.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
