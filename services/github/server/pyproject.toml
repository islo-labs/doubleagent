[project]
name = "github-fake"
version = "1.0.0"
description = "GitHub API fake for DoubleAgent"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
]

[project.scripts]
github-fake = "main:app.run"
